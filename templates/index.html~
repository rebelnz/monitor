{% extends "base.html" %}

{% block body %}

<div id="index-progress-bars">
{% for k,v in sorted(progress_data.iteritems()) %}
  {% module ProgressModule(k,v) %}
{% end %}
</div>

<div class="clear"></div>

<div>
  {% module GraphModule() %}
</div>

<div class="clear"></div>


<div id="process-list">
  <ul>
	{% for p in process_list %}
	{% if p.status == 'running' %}
	<li>{{p.pid}} {{p.name}} {{ p.status }}</li>
	{% end %}
	{% end %}
</ul>
</div>
<div class="clear"></div>


<!-- for progress bars data -->
<script src="{{ static_url("js/loadprogress2.js") }}"></script>

{% end %}

